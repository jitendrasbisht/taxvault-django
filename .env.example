DEBUG=True
SECRET_KEY=change-me-in-production

DB_NAME=taxvault
DB_USER=taxvault
DB_PASSWORD=taxvault
DB_HOST=localhost
DB_PORT=5433

REDIS_HOST=localhost
REDIS_PORT=6379

# Leave EMAIL_BACKEND unset to use the console backend (dev only). Set it to
# django.core.mail.backends.smtp.EmailBackend and fill in the rest once you have real
# SES/Mailgun/Postmark credentials.
EMAIL_BACKEND=
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@taxvault.local
